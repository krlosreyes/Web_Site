// direct-inject.js
// Script disabled: removed automatic injection functionality for security reasons.
// If you still need to import content into Firestore, implement a safe script that
// requires explicit credentials path and confirmation, and does NOT perform any automatic push or deploy.

console.log('direct-inject: disabled.');
process.exit(0);
