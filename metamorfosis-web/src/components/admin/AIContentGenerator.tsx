import React from 'react';

// Este componente existió previamente para generar contenido con IA (Gemini).
// La funcionalidad de generación automática fue eliminada. Se mantiene un
// placeholder ligero para evitar imports rotos y ayudar a migraciones futuras.

const AIContentGenerator = () => {
    return (
        <div className="bg-[#0A0A0A] border border-gray-800 rounded-2xl p-6 shadow-2xl h-full flex items-center justify-center">
            <div className="text-center">
                <h3 className="text-white font-bold">Generación automática deshabilitada</h3>
                <p className="text-sm text-gray-400">La funcionalidad de generación de artículos a partir de transcripciones y la integración con IA han sido eliminadas.</p>
            </div>
        </div>
    );
};

export default AIContentGenerator;
